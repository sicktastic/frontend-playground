const square = x => x * x;

const cube = x => square(x) * x;

const add = (x, y) => {
  return x + y;
}

const add = (x, y) => x + y;


const multiply = (x, y) => {
  return x * y;
}

const multiply = (x, y) => x * y;


const subtract = (x, y) => {
  return x - y;
}

const subtract = (x, y) => x - y;

// function declaration
function divide1 (a, b) {
  return a / b;
}

// function expression
const divide2 = function(a, b) {
  return a / b;
}

// arrow function expression
const divide3 = (a, b) => {
  return a / b ;
}

// arrow function expression simple
const divide4 = (a, b) => a / b;


